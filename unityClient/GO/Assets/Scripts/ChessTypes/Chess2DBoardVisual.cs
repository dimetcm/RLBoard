using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Chess2DBoardVisual : MonoBehaviour
{
    public Texture2D m_lightSquareTexture;
    public Texture2D m_blackSquareTexture;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
